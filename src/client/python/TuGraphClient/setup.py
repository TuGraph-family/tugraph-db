from setuptools import setup

setup(
    name='TuGraphClient',
    version='2.0',
    description='rest client for TuGraph',
    install_requires=[
        'httpx',
    ],
    packages=[''],
    entry_points={}
)
