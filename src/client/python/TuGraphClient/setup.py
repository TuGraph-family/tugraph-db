from setuptools import setup

setup(
    name='TuGraphClient',
    version='1.12.0',
    description='rest client for TuGraph',
    install_requires=[
        'httpx',
        'httpcore',
    ],
    packages=[''],
    entry_points={}
)
